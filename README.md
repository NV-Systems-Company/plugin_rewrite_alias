# plugin_rewrite_alias

#cài module Alias

- sữa function các module xóa $page=1; và $catid=0;
- Sữa các link trong các module theo dạng domain/alias
- add Class CheckAlias để kiểm tra trùng alias toàn site